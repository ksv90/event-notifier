name: Install pnpm
description: Install dependencies and pnpm cache

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v2
      with:
        standalone: true
        run_install: false
        # dest:

    - name: Cache pnpm-store
      uses: actions/cache@v3
      id: pnpm-store
      with:
        path: .pnpm-store
        key: pnpm-store-${{ hashFiles('.npmrc') }}

    - name: Cache node_modules
      uses: actions/cache@v3
      id: node-modules
      with:
        path: node_modules
        key: node-modules-${{ hashFiles('pnpm-lock.yaml') }}

    - name: Set publishing config
      run: pnpm config set '//npm.pkg.github.com/:_authToken' "${NODE_AUTH_TOKEN}"
      shell: bash

    - name: Install dependencies
      if: steps.node-modules.cache-hit != 'true'
      run: pnpm install --frozen-lockfile
      shell: bash

    - name: Test /
      run: ls -a /
      shell: bash

    - name: Test /home
      run: ls -a /home
      shell: bash

    - name: Test /home/runner
      run: ls -a /home/runner
      shell: bash

    - name: Test /home/runner/setup-pnpm
      run: ls -a /home/runner/setup-pnpm
      shell: bash

    - name: Test /home/runner/work
      run: ls -a /home/runner/work
      shell: bash

    - name: Test /home/runner/work/event-notifier
      run: ls -a /home/runner/work/event-notifier
      shell: bash

    - name: Test /home/runner/work/event-notifier/event-notifier
      run: ls -a /home/runner/work/event-notifier/event-notifier
      shell: bash
